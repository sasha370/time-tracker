<tr>
  <td>    <%= post.id %>  </td>
  <td class="text-center">    <%= post.overtime_request %>  </td>
  <td>    <%= post.date %>  </td>
  <td>    <%= post.user.full_name %>  </td>
  <td>    <%= truncate(post.rationale) %>  </td>
<!--  в хелпере прописали отддельный метод, чтобы отображать цветные значки-->
  <td>   <%= status_badge post.status %></td>

  <td>   <%= link_to "Edit", edit_post_path(post), id: "edit_#{post.id}" if policy(post).update? %> </td>
  <td>      <%= link_to "Delete", post_path(post), method: :delete, id: "delete_#{post.id}", data: {confirm: "Are you sure?"} %>
  </td>
</tr>

