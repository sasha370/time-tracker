<!--   Проверяем входит ли текущий пользователь в одну из групп Админов-->
<% if admin_types.include?(current_user.try(:type)) %>
  <%= render 'admin' %>
<% else %>
  <%= render 'employee' %>
<% end %>

